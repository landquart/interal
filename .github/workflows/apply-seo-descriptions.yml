name: Apply SEO descriptions

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-seo-descriptions.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply descriptions
        run: node scripts/apply-seo-descriptions.mjs

      - name: Remove one-time updater
        run: |
          rm scripts/apply-seo-descriptions.mjs
          rm .github/workflows/apply-seo-descriptions.yml

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add SEO descriptions to public pages"
          git push
